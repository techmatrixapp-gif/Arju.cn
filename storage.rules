rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Anyone can read restaurant photos and menu pictures
      allow read: if true;
      // Only authenticated admins can upload/delete files
      allow write: if request.auth != null;
    }
  }
}
